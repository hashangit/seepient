/**
 * T1 — Widget renderer types.
 *
 * Canonical types live in `foundations/contracts/presentation.ts`, shared by
 * the tool producers and the TUI. Re-exported here so TUI components import
 * from one place.
 */

export type { WidgetKind, WidgetAction, WidgetSpec } from '../../../foundations/contracts/presentation.js';
