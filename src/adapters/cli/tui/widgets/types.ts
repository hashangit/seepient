/**
 * T1 — Widget renderer types.
 *
 * Canonical types live in `src/tools/widgets.ts`. Re-exported here so TUI
 * components don't import across the tool/TUI boundary.
 */

export type { WidgetKind, WidgetAction, WidgetSpec } from '../../../../tools/widgets.js';
